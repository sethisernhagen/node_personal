extends layout

block content
  #wrap
    .container
        .row
            h2 #{title}
            br
            p This chart uses the 
                a(href="https://github.com/mbostock/d3/wiki/Pack-Layout", target="_blank") D3.js pack layout 
                | to show the usage statics of a web application. The bubbles represent the firms and users for the application. Clicking on a firm bubble zooms in to show the first names of the users in that firm. You can use the toggle buttons to filter out users that have never logged into the application.
            br
            .text-center
                #btn-group(data-toggle="buttons")
                    label(class="btn btn-default btn-custom active")
                        input(type="radio", name="options", id="option1", autocomplete="off", checked) 
                        | All Users
                    label(class="btn btn-default btn-custom")
                        input(type="radio", name="options", id="option2", autocomplete="off")
                        | Active Users
            #dyno-counts(class="col-md-4 col-lg-5 text-center adoption-count-header")
            #chart-column(class="col-md-8 col-lg-7 text-center")
                #chart(class="adoption-chart")

block scripts
    script(src='http://d3js.org/d3.v3.min.js')
    script(src='/javascripts/d3_tip.js')
    script(src='https://fgnass.github.io/spin.js/spin.min.js')
    script(src='/javascripts/user_bubble_chart.js')
    script(src='/javascripts/bubble_chart.js')